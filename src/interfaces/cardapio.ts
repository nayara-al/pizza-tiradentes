export interface IPizza {
  id: number;
  nome: string;
  preco: number;
  sabores: string[]
  tamanho: string
  descricao: string
}

export interface IDrink {
  id: number;
  nome: string;
  preco: number;
}
